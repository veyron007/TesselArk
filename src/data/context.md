# Frontend data context

- `features.json` is generated from `research/consolidated/feature-register.csv` by `npm run coverage:sync`.
- This catalogue describes proposed scope. Implementation status is maintained separately in the Coverage page; never infer it from research requirement status.
